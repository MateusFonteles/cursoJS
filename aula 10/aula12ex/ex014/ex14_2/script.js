function mostrar () {
    var data = new Date()
    var hora = data.Date.now
    var diaS = data.getDay
    var diaM = data.getDate
    var mes = data.getMonth
    var pais = window.document.getElementsByName('oPais')
    var texto = window.document.getElementById('#dTexto')
    var img = window.document.getElementById('#dImg')
        if  (oPais[1].selected)
            texto.innerHTML ('teste')
        }
